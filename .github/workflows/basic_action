# This workflow is an example to set up the environment and run simple pytests. We want to output a file, i.e. an html file, detailing the test results.

name: Basic Action

on: push 
steps:
- uses: actions/checkout@v4
- name: Set up Python
  uses: actions/setup-python@v4
  with:
    python-version: '3.x'
- name: Install dependencies
  run: python -m pip install --upgrade pip setuptools wheel
  run: |
    python -m pip install --upgrade pip
    pip install -r requirements.txt
  with: 
   verbose: true
   report-title: 'Test Report'
   job-summary: true
   custom-arguments: '-q'
   click-to-expand: true
